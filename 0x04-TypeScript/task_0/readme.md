This task introduces you to the basics of TypeScript. You will set up the project structure, write simple TypeScript code, and compile it into JavaScript using the TypeScript compiler (tsc).

📁 Project Structure
task_0/
│
├── package.json
├── tsconfig.json
├── src/
│   └── main.ts
└── dist/
    └── main.js    (generated after compilation)